# virtmcu Python Requirements
#
# These are used for the Python simulation orchestration tools and testing.
# Pinned versions are REQUIRED for deterministic CI results.
#
# Note: eclipse-zenoh MUST match the version in BUILD_DEPS.

# Core Simulation dependencies
qemu.qmp==0.0.3
eclipse-zenoh==1.9.0

# Development / testing
pytest==8.3.4
pytest-asyncio==1.3.0
pyyaml==6.0.1
hypothesis==6.152.1
ruff==0.15.12

# MCP SDK - used for the AI Debugging interface
mcp==1.0.0

# Formatting / Linting
mypy==1.20.2
codespell==2.4.1
yamllint==1.35.1

# Utilities
psutil==7.2.2
watchdog==6.0.0
fdt==0.3.0
flatbuffers==25.12.19
pyelftools==0.30
lark==1.1.9
typing-inspection==0.4.0
